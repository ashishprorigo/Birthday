server:
  port: 9090
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/sbms1
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: smtp.gmail.com
    password: kfba qryt mfiw vsnd
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: wagha5624@gmail.com
    
logging:
     file:
         max-history: 30
         max-size: 1MB
         name: birthdayapp.log
         total-size-cap: 10MB
     pattern:
         rolling-file-name: birthdayapp-%d{yyyy-MM-dd}.%i.log        
    